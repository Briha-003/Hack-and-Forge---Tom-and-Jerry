version: "3"

services:

postgres:
image: postgres
environment:
POSTGRES_DB: context_ai
POSTGRES_USER: admin
POSTGRES_PASSWORD: admin
ports:
- "5432:5432"

backend:
build: ../backend
ports:
- "8000:8000"
depends_on:
- postgres
